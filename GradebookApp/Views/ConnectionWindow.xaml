<Window x:Class="GradebookApp.Views.ConnectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GradebookApp.Views"
        mc:Ignorable="d"
        Title="Połączenie z bazą danych" Height="450" Width="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Host -->
        <TextBlock Text="Host/IP:" Grid.Row="0" Grid.Column="0" Margin="5"/>
        <TextBox Grid.Row="0" Grid.Column="1" Margin="5"
                 Text="{Binding Host, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" />

        <!-- Port -->
        <TextBlock Text="Port:" Grid.Row="1" Grid.Column="0" Margin="5"/>
        <TextBox Grid.Row="1" Grid.Column="1" Margin="5"
                 Text="{Binding Port, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" />

        <!-- Database -->
        <TextBlock Text="Nazwa bazy:" Grid.Row="2" Grid.Column="0" Margin="5"/>
        <TextBox Grid.Row="2" Grid.Column="1" Margin="5"
                 Text="{Binding DatabaseName, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" />

        <!-- Username -->
        <TextBlock Text="Login:" Grid.Row="3" Grid.Column="0" Margin="5"/>
        <TextBox Grid.Row="3" Grid.Column="1" Margin="5"
                 Text="{Binding Username, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" />

        <!-- Password -->
        <TextBlock Text="Hasło:" Grid.Row="4" Grid.Column="0" Margin="5"/>
        <PasswordBox Grid.Row="4" Grid.Column="1" Margin="5"
                     Name="PasswordBox" PasswordChanged="PasswordBox_PasswordChanged" />

        <!-- Button -->
        <Button Content="Połącz" Grid.Row="5" Grid.ColumnSpan="2" Margin="5"
                HorizontalAlignment="Right" Padding="10,4"
                Command="{Binding ConnectCommand}" />
    </Grid>
</Window>
