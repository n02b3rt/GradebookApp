<Window x:Class="GradebookApp.Views.ManageSubjectsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GradebookApp.Views"
        mc:Ignorable="d"
        Title="ManageSubjectsView" Height="450" Width="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Nagłówek -->
        <TextBlock Text="Moje przedmioty" FontSize="22" FontWeight="Bold" Margin="0 0 0 10"/>

        <!-- Lista przedmiotów -->
        <DataGrid x:Name="SubjectsDataGrid" Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False" Margin="0 10 0 10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Nazwa" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Nauczyciel ID" Binding="{Binding TeacherId}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Przyciski na dole -->
        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Right" Margin="0 10 0 0">
            <Button Content="Dodaj" Style="{StaticResource ButtonStyle}" Click="AddButton_Click"/>
            <Button Content="Edytuj" Style="{StaticResource ButtonStyle}" Click="EditButton_Click"/>
            <Button Content="Usuń" Style="{StaticResource ButtonStyle}" Click="DeleteButton_Click"/>
            <Button Content="Zamknij" Style="{StaticResource ButtonStyle}" Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
