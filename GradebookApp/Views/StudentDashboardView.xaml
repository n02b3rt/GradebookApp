<Window x:Class="GradebookApp.Views.StudentDashboardView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GradebookApp.Views"
        mc:Ignorable="d"
        Title="StudentDashboardView" Height="450" Width="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Powitanie -->
        <TextBlock x:Name="WelcomeTextBlock" FontSize="24" FontWeight="Bold" Margin="0 0 0 20"/>

        <!-- Lista przedmiotów i ocen -->
        <ScrollViewer Grid.Row="1">
            <StackPanel x:Name="SubjectsPanel" />
        </ScrollViewer>
    </Grid>
</Window>
