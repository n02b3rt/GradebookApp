<Window x:Class="GradebookApp.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GradebookApp.Views"
        mc:Ignorable="d"
        Title="LoginView" Height="450" Width="800">
	<Grid Margin="40">
		<Border Background="White" CornerRadius="10" Padding="30" Effect="{DynamicResource {x:Static SystemParameters.DropShadowKey}}">
			<StackPanel VerticalAlignment="Center">
				<TextBlock Text="Zaloguj się" FontSize="24" FontWeight="Bold" Margin="0 0 0 20" HorizontalAlignment="Center"/>

				<TextBlock Text="Login:"/>
				<TextBox x:Name="LoginTextBox" Margin="0 5 0 15" Height="30" />

				<TextBlock Text="Hasło:"/>
				<PasswordBox x:Name="PasswordBox" Margin="0 5 0 15" Height="30" />

				<Button Content="Zaloguj się"
						Style="{StaticResource ButtonStyle}"
                        BorderThickness="0"
                        Cursor="Hand"
                        Click="LoginButton_Click"/>

				<TextBlock x:Name="ErrorText"
                           Text="Błędny login lub hasło"
                           Foreground="Red"
                           Visibility="Collapsed"
                           Margin="0 15 0 0"
                           HorizontalAlignment="Center"/>
			</StackPanel>
		</Border>
	</Grid>
</Window>
